# TeddyCoin v10

Supabase Project URL и publishable key уже вставлены в `SupabaseConfig.kt`.

Важно:
- ключ в приложении должен быть только publishable/anon;
- `secret` / `service_role` key в приложение не вставлять;
- для работы таблицы рейтинга нужно выполнить `SUPABASE_SETUP.sql` в Supabase SQL Editor.
